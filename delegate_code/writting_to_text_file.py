# execute from the command line, ensure cursor is in the directory

file = open("",mode ='w')
# w writes but overites exisiting 
# x creates, raises an error if exists already
# a append to exisiting

#1. write()
text = "\n\additional content\nadditional content\n additional contenet"
file.write(text)
